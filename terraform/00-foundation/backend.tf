# Backend é configurado no root (terraform/backend.tf).
# Este diretório é um módulo consumido pelo root; não possui state próprio.
# Para init/plan/apply use sempre o root: cd terraform && terraform init
