# Módulo 00-foundation
# Propósito: providers, backend opcional, locals (tags/naming), variáveis globais e outputs
# consumidos pelos demais módulos. Apenas data "aws_caller_identity" para account_id; sem recursos aws_*.
