# Subtask 04: Validação e documentação

## Descrição
- Executar `terraform fmt -recursive` e `terraform validate` no módulo 50-lambdas-shell e no root.
- Executar `terraform plan` e revisar as mudanças (novas versões publicadas, event source mappings atualizados).
- Opcional: no README do módulo 50-lambdas-shell, registrar que as Lambdas estão configuradas com SnapStart e published version para redução de cold start.

## Critério de conclusão
- [x] `terraform fmt`, `terraform validate` e `terraform plan` executam sem erro; documentação atualizada se aplicável.
